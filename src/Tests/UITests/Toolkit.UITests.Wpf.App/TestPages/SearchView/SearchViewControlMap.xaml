<local:TestPage
    x:Class="Toolkit.UITests.App.TestPages.SearchViewControlMap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
    xmlns:local="clr-namespace:Toolkit.UITests.App.TestPages"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <esri:MapView x:Name="MyMapView" />
        <Border HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="1" Background="White" BorderBrush="Black">
            <StackPanel Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Scale: 1 / " Margin="1,1,0,1" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ScaleTextBlock" MinWidth="60" FontSize="14" AutomationProperties.AutomationId="ScaleTextBlock" Text="{Binding ElementName=MyMapView, Path=MapScale, Mode=OneWay}"/>
                </StackPanel>
                <Button x:Name="UpdateViewpointExtentToUSA" Content="Update Viewpoint to USA" Padding="1" Click="UpdateViewpointExtentToUSA_Click" AutomationProperties.AutomationId="UpdateViewpointExtentToUSA"/>
                <Button x:Name="UpdateViewpointExtentToOntario" Content="Update Viewpoint to Ontario" Padding="1" Click="UpdateViewpointExtentToOntario_Click" AutomationProperties.AutomationId="UpdateViewpointExtentToOntario"/>
                <Button x:Name="UpdateViewpointExtentToColorado" Content="Update Viewpoint to Colorado" Padding="1" Click="UpdateViewpointExtentToColorado_Click" AutomationProperties.AutomationId="UpdateViewpointExtentToColorado"/>
            </StackPanel>
        </Border>
        <esri:SearchView GeoView="{Binding ElementName=MyMapView}" />
    </Grid>
</local:TestPage>