<local:TestPage 
    x:Class="Toolkit.UITests.App.TestPages.ScaleLines"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
    xmlns:local="clr-namespace:Toolkit.UITests.App.TestPages"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <esri:MapView x:Name="MainMapView" AutomationProperties.AutomationId="MainMapView"/>

        <Border Background="White" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel Margin="5">
                <Button x:Name="WorldEquatorViewpoint"
                        AutomationProperties.AutomationId="WorldEquatorViewpoint"
                        Content="World / Equator"
                        Click="WorldEquatorViewpoint_Click"/>
                <Button x:Name="EquatorDetailViewpoint"
                        AutomationProperties.AutomationId="EquatorDetailViewpoint"
                        Content="Detail / Equator"
                        Click="EquatorDetailViewpoint_Click"/>
                <Button x:Name="HighLatitudeDetailViewpoint"
                        AutomationProperties.AutomationId="HighLatitudeDetailViewpoint"
                        Content="Detail / Latitude 60"
                        Click="HighLatitudeDetailViewpoint_Click"/>
            </StackPanel>
        </Border>

        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,0,0,20" Background="White" BorderBrush="Black" BorderThickness="1">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="Basic Scale Calculation" />
                <esri:ScaleLine MapScale="{Binding MapScale, ElementName=MainMapView, Mode=OneWay}" Grid.Row="1" AutomationProperties.AutomationId="SimpleScaleLine" />

                <TextBlock Text="Advanced Scale Calculation" Grid.Row="2" />
                <esri:ScaleLine MapView="{Binding ElementName=MainMapView}" Grid.Row="3" AutomationProperties.AutomationId="AdvancedScaleLine" />
            </Grid>
        </Border>
    </Grid>
</local:TestPage>
