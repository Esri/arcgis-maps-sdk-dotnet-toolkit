<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:converter="clr-namespace:Esri.ArcGISRuntime.Toolkit.Internal"
    xmlns:system="clr-namespace:System;assembly=System.Runtime">
    <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <system:Double x:Key="CollapsedWidth">48</system:Double>

    <Color x:Key="DefaultBackgroundColor">White</Color>
    <Color x:Key="DefaultForegroundColor">#6e6e6e</Color>
    <Color x:Key="AlternateBackgroundColor">#f3f3f3</Color>
    <Color x:Key="AlternateForegroundColor">#2e2e2e</Color>
    <Color x:Key="SelectedBackgroundColor">#e2f1fb</Color>
    <Color x:Key="SelectedForegroundColor">#005e95</Color>
    <Color x:Key="MutedBorderColor">#6e6e6eaa</Color>

    <SolidColorBrush x:Key="DefaultBackgroundBrush" Color="{StaticResource DefaultBackgroundColor}" />
    <SolidColorBrush x:Key="DefaultForegroundBrush" Color="{StaticResource DefaultForegroundColor}" />
    <SolidColorBrush x:Key="AlternateBackgroundBrush" Color="{StaticResource AlternateBackgroundColor}" />
    <SolidColorBrush x:Key="AlternateForegroundBrush" Color="{StaticResource AlternateForegroundColor}" />
    <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="{StaticResource SelectedBackgroundColor}" />
    <SolidColorBrush x:Key="SelectedForegroundBrush" Color="{StaticResource SelectedForegroundColor}" />
    <SolidColorBrush x:Key="MutedBorderBrush" Color="{StaticResource MutedBorderColor}" />

    <Geometry x:Key="BrowseBuildingsIcon">M13 9h-1V6h4v10h-1V7h-2zM5 5v5h1V3h1V0h2.62L11 1.867V9h-1V2.197L9.115 1H8v3H7v12H0V7h1V5zm-2 6v-1h1V6H2v2H1v7h5v-4zm7-1h4v6H8v-4h2zm1 1v2H9v2h4v-4zm-8 2h2v-1H3z</Geometry>
    <Geometry x:Key="ChevronLeft">M10.207 4l-4 4 4 4H8.793l-4-4 4-4z</Geometry>
    <Geometry x:Key="CloseIcon">M3.98 11.303L7.281 8 3.98 4.697l.707-.707L7.99 7.293l.01-.01.01.01 3.304-3.303.707.707L8.718 8l3.303 3.303-.707.707L8.01 8.707l-.01.01-.01-.01-3.304 3.303z</Geometry>
    <Geometry x:Key="CollapseIcon">M7.793 12l4-4-4-4h1.414l4 4-4 4zm-4.586 0l4-4-4-4H1.793l4 4-4 4z</Geometry>
    <Geometry x:Key="ExpandIcon">M8.207 4l-4 4 4 4H6.793l-4-4 4-4zm4.586 0l-4 4 4 4h1.414l-4-4 4-4z</Geometry>
    <Geometry x:Key="SearchIcon">M13.936 13.24L9.708 9.01a4.8 4.8 0 1 0-.69.69l4.228 4.228a.488.488 0 0 0 .69-.69zM6.002 9.8A3.8 3.8 0 1 1 8.69 8.686a3.778 3.778 0 0 1-2.687 1.112z</Geometry>
    <Geometry x:Key="ZoomToIcon">M15.293 16L12 12.707V15h-1v-4h4v1h-2.293L16 15.293zm0-16L12 3.293V1h-1v4h4V4h-2.293L16 .707zM8 6a2 2 0 1 1-2 2 2 2 0 0 1 2-2zm0 1a1 1 0 1 0 1 1 1 1 0 0 0-1-1zm-7 5h2.293L0 15.293.707 16 4 12.707V15h1v-4H1zm3-8.707L.707 0 0 .707 3.293 4H1v1h4V1H4z</Geometry>
    <Geometry x:Key="AllIcon">M.088.482A.358.358 0 0 1 .305.205a5.075 5.075 0 0 1 4.104.6c.031.022.06.047.091.069L2.704 3.651c-.023-.013-.046-.022-.068-.037a1.407 1.407 0 0 1-.527-2.009 1.599 1.599 0 0 1 1.227-.604 3.737 3.737 0 0 0-2.6-.099 3.455 3.455 0 0 0 1.647 3.244l-.376.581A3.95 3.95 0 0 1 .088.482zM4 7V6H3v1zm2-6H5v1h1zm2 1H7v1h1zm2 1H9v1h1zM6 8H5v1h1zm-4 5h1v-1H2zm2-1v1h1v-1zm9-5h1V6h-1zm0 2h1V8h-1zm0 2h1v-1h-1zm0 2h1v-1h-1zm-2-4h1V8h-1zm0 2h1v-1h-1zm0 2h1v-1h-1zM9 6v9H6.969v-5H0v6h16V1.002zm-3 9H1v-4h5zm9 0h-5V6.515l5-3.57z</Geometry>
    <Style x:Key="BrowseTabControlStyle" TargetType="{x:Type TabControl}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="SelectedIndex" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Border
                        x:Name="Border"
                        BorderThickness="0"
                        CornerRadius="0">
                        <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PathIcon" TargetType="Path">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Stretch" Value="None" />
    </Style>
    <ControlTemplate x:Key="BrowseButtonTemplate" TargetType="ToggleButton">
        <Border
            x:Name="Border"
            Height="48"
            Background="{StaticResource DefaultBackgroundBrush}"
            CornerRadius="0">
            <ContentPresenter
                x:Name="Presenter"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <!--  Default/Normal  -->
            <!--  Mouse over, unchecked  -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
                <Setter TargetName="Presenter" Property="TextElement.Foreground" Value="{StaticResource AlternateForegroundBrush}" />
            </Trigger>
            <!--  Pressed, unchecked  -->
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
                <Setter TargetName="Presenter" Property="TextElement.Foreground" Value="{StaticResource AlternateForegroundBrush}" />
            </Trigger>
            <!--  Checked all  -->
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
                <Setter Property="Foreground" Value="{StaticResource SelectedForegroundBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="AllFloorsButtonStyle" TargetType="ToggleButton">
        <Setter Property="Height" Value="{StaticResource CollapsedWidth}" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Template" Value="{StaticResource BrowseButtonTemplate}" />
    </Style>

    <Style x:Key="TabRadioStyle" TargetType="RadioButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Border" Background="{TemplateBinding Background}">
                        <ContentPresenter
                            x:Name="Presenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Center"
                            TextBlock.FontSize="{TemplateBinding FontSize}"
                            TextBlock.Foreground="{TemplateBinding Foreground}" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.2" />
                                    <VisualTransition GeneratedDuration="0" To="Pressed" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource AlternateBackgroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Presenter" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource AlternateForegroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource SelectedBackgroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Presenter" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource AlternateForegroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.2" />
                                    <VisualTransition GeneratedDuration="0" To="Pressed" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Unchecked" />
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource SelectedBackgroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Presenter" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource SelectedForegroundColor}" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <DataTemplate x:Key="LevelDataTemplate">
        <Border x:Name="Container" Height="{StaticResource CollapsedWidth}">
            <Grid>

                <TextBlock
                    x:Name="CollapsedTemplate"
                    Padding="8,8,8,8"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="{Binding ShortName, Mode=OneTime}" />
                <TextBlock
                    x:Name="ExpandedTemplate"
                    Padding="16,8,8,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{Binding LongName, Mode=OneTime}"
                    TextAlignment="Left"
                    Visibility="Collapsed" />
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <Trigger Property="controls:FloorFilter.IsExpanded" Value="False">
                <Setter TargetName="Container" Property="Width" Value="{StaticResource CollapsedWidth}" />
            </Trigger>
            <Trigger Property="controls:FloorFilter.IsExpanded" Value="True">
                <Setter TargetName="CollapsedTemplate" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="ExpandedTemplate" Property="Visibility" Value="Visible" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>
    <DataTemplate x:Key="SiteDataTemplate">
        <Border Height="{StaticResource CollapsedWidth}">
            <TextBlock
                Padding="8"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Text="{Binding Name, Mode=OneTime}" />
        </Border>
    </DataTemplate>
    <DataTemplate x:Key="FacilityDataTemplate">
        <Border MinHeight="{StaticResource CollapsedWidth}">
            <TextBlock
                Padding="8,8,8,4"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Text="{Binding Name, Mode=OneTime}" />
        </Border>
    </DataTemplate>
    <DataTemplate x:Key="DifferentiatingFacilityDataTemplate">
        <Border MinHeight="{StaticResource CollapsedWidth}">
            <StackPanel VerticalAlignment="Center">
                <TextBlock
                    Padding="8,8,8,4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Text="{Binding Name, Mode=OneTime}" />
                <TextBlock
                    Padding="8,0,8,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="10"
                    Text="{Binding Site.Name, Mode=OneTime}" />
            </StackPanel>
        </Border>
    </DataTemplate>
    <ControlTemplate x:Key="CloseButtonTemplate" TargetType="Button">
        <Border
            x:Name="Border"
            Background="{TemplateBinding Background}"
            BorderThickness="0"
            CornerRadius="0">
            <ContentPresenter
                x:Name="Presenter"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                <Setter Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                <Setter Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="ClearButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
        <Setter Property="Template" Value="{StaticResource CloseButtonTemplate}" />
    </Style>
    <Style x:Key="CloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Template" Value="{StaticResource CloseButtonTemplate}" />
    </Style>

    <Style x:Key="TabToggleStyle" TargetType="ButtonBase">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonBase">
                    <Border
                        x:Name="Border"
                        Padding="{TemplateBinding Padding}"
                        Background="{StaticResource DefaultBackgroundBrush}"
                        CornerRadius="0">
                        <ContentPresenter
                            x:Name="Presenter"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  Default/Normal  -->
                        <!--  Mouse over, unchecked  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
                            <Setter TargetName="Presenter" Property="TextElement.Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                        </Trigger>
                        <!--  Pressed, unchecked  -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
                            <Setter TargetName="Presenter" Property="TextElement.Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                        </Trigger>
                        <!--  Checked all  -->
                        <Trigger Property="ToggleButton.IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource SelectedForegroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CommonListStyle" TargetType="ListView">
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border
                                    x:Name="Border"
                                    Margin="0,0,0,0"
                                    Background="{StaticResource DefaultBackgroundBrush}"
                                    SnapsToDevicePixels="true">
                                    <ContentPresenter Margin="0" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
                                        <Setter TargetName="Border" Property="TextBlock.Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
                                        <Setter TargetName="Border" Property="TextBlock.FontWeight" Value="SemiBold" />
                                        <Setter TargetName="Border" Property="TextBlock.Foreground" Value="{StaticResource SelectedForegroundBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="FloorFilterTemplate" TargetType="controls:FloorFilter">
        <Grid x:Name="PART_AutoVisibilityWrapper" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!--  Main picker  -->
            <Border
                x:Name="BarContainer"
                VerticalAlignment="{TemplateBinding VerticalAlignment}"
                BorderBrush="{StaticResource MutedBorderBrush}"
                BorderThickness="1">

                <Grid>
                    <Grid.RowDefinitions>
                        <!--  Browse button  -->
                        <RowDefinition Height="Auto" />
                        <!--  All floors button  -->
                        <RowDefinition Height="Auto" />
                        <!--  Floor List  -->
                        <RowDefinition Height="Auto" />
                        <!--  Zoom button  -->
                        <RowDefinition Height="Auto" />
                        <!--  Expand/Collapse Button  -->
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ToggleButton
                        x:Name="BrowseToggle"
                        Grid.Row="0"
                        HorizontalContentAlignment="Stretch"
                        IsChecked="{Binding IsBrowseOpen, Mode=TwoWay}"
                        Style="{TemplateBinding BrowseButtonStyle}"
                        ToolTip="{TemplateBinding BrowseLabel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Path
                                Data="{StaticResource BrowseBuildingsIcon}"
                                Fill="{Binding ElementName=BrowseToggle, Path=Foreground, Mode=OneWay}"
                                Style="{StaticResource PathIcon}" />
                            <Grid Grid.Column="1" Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock
                                    Margin="-8,0,8,0"
                                    HorizontalAlignment="Left"
                                    Text="Browse" />
                            </Grid>
                        </Grid>
                    </ToggleButton>
                    <ToggleButton
                        x:Name="AllButton"
                        Grid.Row="1"
                        HorizontalContentAlignment="Stretch"
                        IsChecked="{Binding AllLevelsSelected, Mode=TwoWay}"
                        Style="{TemplateBinding BrowseButtonStyle}"
                        ToolTip="{TemplateBinding AllFloorsLabel}"
                        Visibility="{Binding ShowAllFloorsButton, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Path
                                Data="{StaticResource AllIcon}"
                                Fill="{Binding ElementName=AllButton, Path=Foreground, Mode=OneWay}"
                                Style="{StaticResource PathIcon}" />
                            <Grid Grid.Column="1" Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock
                                    Margin="-8,0,0,0"
                                    HorizontalAlignment="Left"
                                    Text="All"
                                    TextAlignment="Left" />
                            </Grid>
                        </Grid>
                    </ToggleButton>
                    <ListView
                        Grid.Row="2"
                        ItemTemplate="{TemplateBinding LevelDataTemplate}"
                        ItemsSource="{Binding AllLevels, Mode=OneWay}"
                        SelectedItem="{Binding SelectedLevel, Mode=TwoWay}"
                        Style="{TemplateBinding CommonListStyle}"
                        Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='Inverse'}" />
                    <ListView
                        Grid.Row="2"
                        ItemTemplate="{TemplateBinding LevelDataTemplate}"
                        ItemsSource="{Binding AllLevels, Mode=OneWay}"
                        SelectedItem="{Binding SelectedLevel, Mode=TwoWay}"
                        Style="{TemplateBinding CommonListStyle}"
                        Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button
                        x:Name="ZoomButton"
                        Grid.Row="3"
                        HorizontalContentAlignment="Stretch"
                        Command="{Binding ZoomCommand, Mode=OneTime}"
                        Style="{TemplateBinding ZoomToButtonStyle}"
                        ToolTip="{TemplateBinding ZoomToLabel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Path
                                Data="{StaticResource ZoomToIcon}"
                                Fill="{Binding ElementName=ZoomButton, Path=Foreground, Mode=OneWay}"
                                Style="{StaticResource PathIcon}" />
                            <Grid Grid.Column="1" Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock
                                    Margin="-8,0,8,0"
                                    Foreground="{Binding ElementName=ZoomButton, Path=Foreground, Mode=OneWay}"
                                    Text="{TemplateBinding ZoomToLabel}" />
                            </Grid>
                        </Grid>
                    </Button>
                    <!--  TODO: update tooltip to match state  -->
                    <ToggleButton
                        Grid.Row="4"
                        IsChecked="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=TwoWay}"
                        Style="{TemplateBinding ExpandCollapseButtonStyle}"
                        ToolTip="{TemplateBinding ExpandLabel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Path
                                x:Name="CollapseIcon"
                                Height="12"
                                HorizontalAlignment="Center"
                                Fill="{StaticResource DefaultForegroundBrush}"
                                Stretch="Uniform"
                                Style="{StaticResource PathIcon}"
                                Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <Path
                                x:Name="ExpandIcon"
                                Height="12"
                                HorizontalAlignment="Center"
                                Fill="{StaticResource DefaultForegroundBrush}"
                                Stretch="Uniform"
                                Style="{StaticResource PathIcon}"
                                Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='Inverse'}" />
                            <TextBlock
                                Grid.Column="1"
                                Margin="-8,0,16,0"
                                Foreground="{StaticResource DefaultForegroundBrush}"
                                Text="Collapse"
                                Visibility="{Binding Path=(controls:FloorFilter.IsExpanded), Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </Grid>
                    </ToggleButton>
                </Grid>
            </Border>
            <Border
                x:Name="BrowseContainer"
                MaxWidth="250"
                Margin="8,0,8,0"
                VerticalAlignment="{TemplateBinding VerticalAlignment}"
                Background="{StaticResource DefaultBackgroundBrush}"
                BorderBrush="{StaticResource MutedBorderBrush}"
                BorderThickness="1"
                Visibility="{Binding IsBrowseOpen, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button
                        x:Name="PART_BackButton"
                        VerticalAlignment="Stretch"
                        Command="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=GoBackCommand}"
                        Style="{StaticResource CloseButtonStyle}"
                        ToolTip="{TemplateBinding BackButtonLabel}"
                        Visibility="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsEnabled, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
                        <Path
                            Data="{StaticResource ChevronLeft}"
                            Fill="{StaticResource DefaultForegroundBrush}"
                            Style="{StaticResource PathIcon}" />
                    </Button>
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="4"
                        VerticalAlignment="Center">

                        <TextBlock
                            FontSize="16"
                            FontWeight="SemiBold"
                            Text="{TemplateBinding BrowseLabel}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            Text="{Binding SelectedSite.Name, Mode=OneWay}"
                            TextWrapping="Wrap"
                            Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=ShowSiteNameSubtitle, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>
                    <Button
                        Grid.Column="2"
                        Width="32"
                        MinHeight="32"
                        Margin="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Stretch"
                        Command="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=CloseBrowseCommand}"
                        Style="{StaticResource CloseButtonStyle}"
                        ToolTip="{TemplateBinding CloseLabel}">
                        <Path
                            Data="{StaticResource CloseIcon}"
                            Fill="{StaticResource DefaultForegroundBrush}"
                            Style="{StaticResource PathIcon}" />
                    </Button>
                    <!--  Browse Facilities/Sites  -->
                    <TabControl
                        Grid.Row="2"
                        Grid.ColumnSpan="3"
                        MinWidth="200"
                        MaxHeight="300"
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        BorderThickness="0"
                        SelectedIndex="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SelectedBrowseTab}"
                        Style="{StaticResource BrowseTabControlStyle}">
                        <TabItem Header="Sites">
                            <Grid Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <controls:FilteringListView
                                    x:Name="PART_SiteListView"
                                    Grid.Row="0"
                                    ItemTemplate="{TemplateBinding SiteDataTemplate}"
                                    ItemsSource="{Binding AllSites, Mode=OneWay}"
                                    NoResultsMessage="{TemplateBinding NoResultsMessage}"
                                    Placeholder="{TemplateBinding SearchPlaceholder}"
                                    RememberingSelectedItem="{Binding SelectedSite, Mode=TwoWay}"
                                    Style="{StaticResource CommonListStyle}" />
                                <Button
                                    x:Name="PART_AllButton"
                                    Grid.Row="1"
                                    Command="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=ShowAllFacilitiesCommand}"
                                    Content="{TemplateBinding AllFacilitiesLabel}"
                                    Style="{StaticResource TabToggleStyle}" />
                            </Grid>
                        </TabItem>
                        <TabItem Header="Facilities">
                            <controls:FilteringListView
                                x:Name="PART_FacilitiesListView"
                                Grid.Row="1"
                                ItemTemplate="{TemplateBinding FacilityDataTemplate}"
                                ItemsSource="{Binding SelectedSite.Facilities}"
                                NoResultsMessage="{TemplateBinding NoResultsMessage}"
                                Placeholder="{TemplateBinding SearchPlaceholder}"
                                RememberingSelectedItem="{Binding SelectedFacility, Mode=TwoWay}"
                                Style="{StaticResource CommonListStyle}" />
                        </TabItem>
                        <TabItem Header="All">
                            <controls:FilteringListView
                                x:Name="PART_AllFacilitiesListView"
                                ItemTemplate="{TemplateBinding DifferentiatingFacilityDataTemplate}"
                                ItemsSource="{Binding AllFacilities}"
                                NoResultsMessage="{TemplateBinding NoResultsMessage}"
                                Placeholder="{TemplateBinding SearchPlaceholder}"
                                RememberingSelectedItem="{Binding SelectedFacility, Mode=TwoWay}"
                                Style="{StaticResource CommonListStyle}" />
                        </TabItem>
                        <TabItem Header="NoSites">
                            <controls:FilteringListView
                                x:Name="PART_FacilitiesNoSitesListView"
                                ItemTemplate="{StaticResource FacilityDataTemplate}"
                                ItemsSource="{Binding AllFacilities}"
                                NoResultsMessage="{TemplateBinding NoResultsMessage}"
                                Placeholder="{TemplateBinding SearchPlaceholder}"
                                RememberingSelectedItem="{Binding SelectedFacility, Mode=TwoWay}"
                                Style="{StaticResource CommonListStyle}" />
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HorizontalAlignment" Value="Left">
                <Setter TargetName="BrowseContainer" Property="Grid.Column" Value="1" />
                <Setter TargetName="BarContainer" Property="Grid.Column" Value="0" />
                <Setter TargetName="ExpandIcon" Property="Data" Value="{StaticResource CollapseIcon}" />
                <Setter TargetName="CollapseIcon" Property="Data" Value="{StaticResource ExpandIcon}" />
            </Trigger>
            <Trigger Property="HorizontalAlignment" Value="Right">
                <Setter TargetName="BrowseContainer" Property="Grid.Column" Value="0" />
                <Setter TargetName="BarContainer" Property="Grid.Column" Value="1" />
                <Setter TargetName="ExpandIcon" Property="Data" Value="{StaticResource ExpandIcon}" />
                <Setter TargetName="CollapseIcon" Property="Data" Value="{StaticResource CollapseIcon}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ExpandCollapseButtonTemplate" TargetType="ToggleButton">
        <Border
            x:Name="Border"
            Background="{TemplateBinding Background}"
            BorderBrush="{StaticResource AlternateBackgroundBrush}"
            BorderThickness="0,1,0,0"
            CornerRadius="0">
            <ContentPresenter
                x:Name="Presenter"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsChecked" Value="False">
                <!--  To do: localize  -->
                <Setter Property="ToolTip" Value="Expand" />
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="True">
                <Setter Property="ToolTip" Value="Collapse" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                <Setter Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AlternateForegroundBrush}" />
                <Setter Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ZoomToButtonTemplate" TargetType="Button">
        <Border
            x:Name="Border"
            Padding="0,0,0,0"
            Background="{TemplateBinding Background}"
            BorderBrush="{StaticResource AlternateBackgroundBrush}"
            BorderThickness="0,1,0,0"
            CornerRadius="0">
            <ContentPresenter
                x:Name="Presenter"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource AlternateBackgroundBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectedBackgroundBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ExpandCollapseButtonStyle" TargetType="ToggleButton">
        <Setter Property="MinWidth" Value="{StaticResource CollapsedWidth}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Template" Value="{StaticResource ExpandCollapseButtonTemplate}" />
    </Style>
    <Style x:Key="BrowseButtonStyle" TargetType="ToggleButton">
        <Setter Property="Height" Value="{StaticResource CollapsedWidth}" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Template" Value="{StaticResource BrowseButtonTemplate}" />
    </Style>

    <Style x:Key="ZoomToButtonStyle" TargetType="Button">
        <Setter Property="MinWidth" Value="{StaticResource CollapsedWidth}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Foreground" Value="{StaticResource DefaultForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource DefaultBackgroundBrush}" />
        <Setter Property="Template" Value="{StaticResource ZoomToButtonTemplate}" />
    </Style>
    <Style TargetType="{x:Type controls:FilteringListView}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:FilteringListView}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid
                                Grid.Row="0"
                                Background="{StaticResource AlternateBackgroundBrush}"
                                Visibility="{Binding Path=HasSearchCapability, RelativeSource={RelativeSource AncestorType=controls:FilteringListView}, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Path
                                    Grid.Column="0"
                                    Margin="8,0,0,0"
                                    Data="{StaticResource SearchIcon}"
                                    Fill="{StaticResource DefaultForegroundBrush}"
                                    Style="{StaticResource PathIcon}" />
                                <TextBox
                                    Grid.Column="1"
                                    Height="40"
                                    Margin="0"
                                    Padding="8,4,8,4"
                                    VerticalContentAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Text="{Binding Path=SearchString, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock
                                    x:Name="PART_Placeholder"
                                    Grid.Column="1"
                                    Margin="0"
                                    Padding="8,4,8,4"
                                    VerticalAlignment="Center"
                                    Foreground="{StaticResource AlternateForegroundBrush}"
                                    IsHitTestVisible="False"
                                    Text="{TemplateBinding Placeholder}" />
                                <Button
                                    x:Name="PART_ClearButton"
                                    Grid.Column="1"
                                    Width="40"
                                    Height="40"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource ClearButtonStyle}"
                                    Visibility="Collapsed">
                                    <Path
                                        Data="{StaticResource CloseIcon}"
                                        Fill="{StaticResource AlternateForegroundBrush}"
                                        Style="{StaticResource PathIcon}" />
                                </Button>
                            </Grid>
                            <ScrollViewer
                                Grid.Row="1"
                                HorizontalScrollBarVisibility="Disabled"
                                VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                            <TextBlock
                                x:Name="PART_NoResultsLabel"
                                Grid.Row="1"
                                Margin="8"
                                Text="{TemplateBinding NoResultsMessage}"
                                TextAlignment="Center"
                                Visibility="Collapsed" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="controls:FloorFilter">
        <Setter Property="BrowseLabel" Value="Browse" />
        <Setter Property="CloseLabel" Value="Close" />
        <Setter Property="CollapseLabel" Value="Collapse" />
        <Setter Property="ExpandLabel" Value="Expand" />
        <Setter Property="ZoomToLabel" Value="Zoom To" />
        <Setter Property="NoResultsMessage" Value="No results found." />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="AllFacilitiesLabel" Value="All" />
        <Setter Property="BackButtonLabel" Value="Go Back" />
        <Setter Property="AllFloorsLabel" Value="All Floors" />
        <Setter Property="SearchPlaceholder" Value="Search" />
        <Setter Property="Margin" Value="8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="controls:FloorFilter.IsExpanded" Value="False" />
        <Setter Property="LevelDataTemplate" Value="{StaticResource LevelDataTemplate}" />
        <Setter Property="SiteDataTemplate" Value="{StaticResource SiteDataTemplate}" />
        <Setter Property="FacilityDataTemplate" Value="{StaticResource FacilityDataTemplate}" />
        <Setter Property="DifferentiatingFacilityDataTemplate" Value="{StaticResource DifferentiatingFacilityDataTemplate}" />
        <Setter Property="CommonListStyle" Value="{StaticResource CommonListStyle}" />
        <Setter Property="ExpandCollapseButtonStyle" Value="{StaticResource ExpandCollapseButtonStyle}" />
        <Setter Property="BrowseButtonStyle" Value="{StaticResource BrowseButtonStyle}" />
        <Setter Property="ZoomToButtonStyle" Value="{StaticResource ZoomToButtonStyle}" />
        <Setter Property="Template" Value="{StaticResource FloorFilterTemplate}" />
    </Style>
</ResourceDictionary>