<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
    xmlns:internal="clr-namespace:Esri.ArcGISRuntime.Toolkit.Internal">
    <Style TargetType="{x:Type controls:OfflineMapAreasView}">
        <!--  Default item template can be overriden by the user.  -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:OfflineMapAreasView}">
                    <Border
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}">
                            <StackPanel>
                                <ProgressBar IsIndeterminate="{Binding TemplateSettings.IsLoadingModels, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding OnlineMap.Item.Title, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    <TextBlock Text="(Online)" />
                                </StackPanel>
                                <TextBlock Text="On-Demand Map Areas" />
                                <ListView x:Name="ListView" SelectionMode="Single"
                                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                          ItemsSource="{Binding TemplateSettings.OnDemandMapModels, RelativeSource={RelativeSource Mode=TemplatedParent}}" >
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <TextBlock Text="Preplanned Map Areas" />
                                <ListView x:Name="ListView2" SelectionMode="Single"
                                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                        ItemsSource="{Binding TemplateSettings.PreplannedMapModels, RelativeSource={RelativeSource Mode=TemplatedParent}}" >
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" />
                                                <CheckBox Content="IsDownloaded" IsChecked="{Binding IsDownloaded, Mode=OneWay}" IsEnabled="False" />
                                                <CheckBox Content="AllowsDownload" IsChecked="{Binding AllowsDownload, Mode=OneWay}" IsEnabled="False" />
                                                <TextBlock Text="{Binding SizeInBytes}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
    </Style>
</ResourceDictionary>
