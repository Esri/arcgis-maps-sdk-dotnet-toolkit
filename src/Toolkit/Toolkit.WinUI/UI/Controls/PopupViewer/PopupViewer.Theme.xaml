<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:internal="using:Esri.ArcGISRuntime.Toolkit.Internal"
                    xmlns:controls="using:Esri.ArcGISRuntime.Toolkit.UI.Controls"
                    xmlns:primitives="using:Esri.ArcGISRuntime.Toolkit.Primitives"
                    xmlns:popup="using:Esri.ArcGISRuntime.Mapping.Popups">

    <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Default">
            <SolidColorBrush x:Key="FieldsPopupElementViewBackgroundRowOddBrush">#FF121212</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewBackgroundRowEvenBrush">#FF040404</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewDividerBrush">#11CCCCCC</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewForegroundBrush">#FFCDCDCD</SolidColorBrush>
        </ResourceDictionary>
        <ResourceDictionary x:Key="Light">
            <SolidColorBrush x:Key="FieldsPopupElementViewBackgroundRowOddBrush">#FFEDEDED</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewBackgroundRowEvenBrush">#FFFBFBFB</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewDividerBrush">#11333333</SolidColorBrush>
            <SolidColorBrush x:Key="FieldsPopupElementViewForegroundBrush">#FF323232</SolidColorBrush>
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../../Themes/SharedResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <internal:VisibilityConverter x:Key="PopupViewerVisibilityConverter"/>
    <internal:EmptyStringToVisibilityConverter x:Key="PlaceholderVisibilityConverter" />
    <internal:HtmlToPlainTextConverter x:Key="PopupViewerHtmlToPlainTextConverter"/>
    <Style TargetType="TextBlock" x:Key="PopupViewerHeaderStyle">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style TargetType="TextBlock" x:Key="PopupViewerTitleStyle">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style TargetType="TextBlock" x:Key="PopupViewerCaptionStyle">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="Gray"/>
    </Style>
    <Style TargetType="primitives:TextPopupElementView">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:TextPopupElementView">
                    <ContentControl x:Name="TextArea" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TextBlock" x:Key="FieldsPopupElementViewTextStyle">
        <Setter Property="Margin" Value="7" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{ThemeResource FieldsPopupElementViewForegroundBrush}" />
        <Setter Property="IsTextSelectionEnabled" Value="True" />
    </Style>
    <Style TargetType="primitives:FieldsPopupElementView" >
        <Setter Property="RowOddBackground" Value="{ThemeResource FieldsPopupElementViewBackgroundRowOddBrush}" />
        <Setter Property="RowEvenBackground" Value="{ThemeResource FieldsPopupElementViewBackgroundRowEvenBrush}" />
        <Setter Property="DividerBrush" Value="{ThemeResource FieldsPopupElementViewDividerBrush}" />
        <Setter Property="FieldTextStyle" Value="{StaticResource FieldsPopupElementViewTextStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:FieldsPopupElementView">
                    <StackPanel>
                        <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Mode=OneWay}" Style="{StaticResource PopupViewerCaptionStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <ContentPresenter x:Name="TableAreaContent" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:AttachmentThumbnailImage" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:AttachmentThumbnailImage">
                    <StackPanel Orientation="Horizontal">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="LoadedStates">
                                <VisualState x:Name="AttachmentLoading">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadSpinner.Visibility" Value="Visible" />
                                        <Setter Target="DownloadSpinner.IsActive" Value="True" />
                                        <Setter Target="Icon.Text" Value="" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="AttachmentLoaded">
                                    <VisualState.Setters>
                                        <Setter Target="Icon.Text" Value="&#xF13E;" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="AttachmentFailedToLoad">
                                    <VisualState.Setters>
                                        <Setter Target="Icon.Text" Value="&#xEA6A;" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="NotLoaded">
                                    <VisualState.Setters>
                                        <Setter Target="Icon.Text" Value="&#xE896;" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="AttachmentIsLocal">
                                    <VisualState.Setters>
                                        <Setter Target="Icon.Text" Value="" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid Width="{TemplateBinding ThumbnailSize}" Height="{TemplateBinding ThumbnailSize}">
                            <TextBlock x:Name="Icon" FontFamily="Segoe MDL2 Assets" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,0" Text="&#xE896;" />
                            <Image x:Name="PART_Image" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <ProgressRing Visibility="Collapsed" x:Name="DownloadSpinner" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Attachment.Name, Mode=OneTime}" Margin="5" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:AttachmentsPopupElementView" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:AttachmentsPopupElementView">
                    <StackPanel>
                        <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Mode=OneWay}" Style="{StaticResource PopupViewerCaptionStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <ListView x:Name="AttachmentList" BorderThickness="0" SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <primitives:AttachmentThumbnailImage Attachment="{Binding}" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:MediaPopupElementView" >
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Margin="0,0,0,0" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <primitives:PopupMediaView PopupMedia="{Binding}" Margin="0,5" VerticalAlignment="Center" />
                        <TextBlock Grid.Row="1" Text="{Binding Title, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}" Visibility="{Binding Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <TextBlock Grid.Row="2" Text="{Binding Caption, Mode=OneWay}" Style="{StaticResource PopupViewerCaptionStyle}" Visibility="{Binding Caption, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:MediaPopupElementView">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <TextBlock Grid.Row="1" Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Mode=OneWay}" Style="{StaticResource PopupViewerCaptionStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <FlipView Background="Transparent" MaxHeight="200" x:Name="FlipView" ItemTemplate="{TemplateBinding ItemTemplate}" ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Media}" Grid.Row="2"/>
                        <ContentControl x:Name="PipsPagerContainer" Grid.Row="3" Visibility="Collapsed" HorizontalAlignment="Center" />
                        <!-- Without UWP support we could have used PipsPager directly here: <PipsPager x:Name="PipsPager"
                            HorizontalAlignment="Center"
                            Grid.Row="3"
                            NumberOfPages="{Binding Media.Count}"
                            SelectedPageIndex="{Binding SelectedIndex, ElementName=FlipView, Mode=TwoWay}" /> -->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:UtilityAssociationsPopupElementView" >
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <!-- x:DataType=UtilityAssociationsFilterResults-->
                    <primitives:UtilityAssociationsFilterResultsPopupView AssociationsFilterResult="{Binding}" x:Name="ResultView" MinHeight="40" AutomationProperties.Name="{Binding Filter.Title}" IsExpanded="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:UtilityAssociationsPopupElementView">
                    <StackPanel>
                        <TextBlock x:Name="Title" Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Mode=OneWay, FallbackValue={internal:LocalizedString Key=PopupViewerUtilityAssociationsDefaultTitle}}" TextTrimming="CharacterEllipsis" Style="{StaticResource PopupViewerTitleStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <TextBlock Grid.Row="1" Text="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Mode=OneWay}" TextTrimming="CharacterEllipsis" Style="{StaticResource PopupViewerCaptionStyle}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.Description, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                        <ListView VerticalAlignment="Top" ItemTemplate="{TemplateBinding ItemTemplate}" ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Element.AssociationsFilterResults, Mode=OneWay}"
                                  x:Name="AssociationsList" SelectionMode="None" IsItemClickEnabled="True">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                        <Grid x:Name="NoAssociationsGrid" Margin="10,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border BorderThickness="0" Width="1" Background="Red" Margin="2" />
                            <FontIcon Glyph="&#xE7BA;" Foreground="Red" Margin="5" Grid.Column="1" VerticalAlignment="Center" />
                            <TextBlock Text="{internal:LocalizedString Key=PopupViewerUtilityAssociationsNone}" VerticalAlignment="Center" Grid.Column="2" Margin="5" />
                        </Grid>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:UtilityAssociationsFilterResultsPopupView">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <!-- x:DataType=UtilityAssociationGroupResult-->
                    <primitives:UtilityAssociationGroupResultPopupView GroupResult="{Binding}" x:Name="ResultView" MinHeight="40" AutomationProperties.Name="{Binding Name}" IsSearchable="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:UtilityAssociationsFilterResultsPopupView">
                    <Grid Margin="10,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Filter.Title, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}" TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <TextBlock Grid.Row="1" Text="{Binding Filter.Description, Mode=OneWay}" Style="{StaticResource PopupViewerCaptionStyle}" TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <FontIcon Glyph="&#xE76C;" Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Center" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <Border BorderThickness="0" Height="1" Background="LightGray" Margin="2" Grid.Row="2" Grid.ColumnSpan="2" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <ItemsControl VerticalAlignment="Top" ItemTemplate="{TemplateBinding ItemTemplate}" ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=AssociationsFilterResult.GroupResults}"
                                  Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Converter={StaticResource PopupViewerVisibilityConverter}}" 
                                  x:Name="ResultsList" ItemContainerTransitions="{x:Null}" Background="{ThemeResource SystemChromeAltHighColor}" CornerRadius="4" Grid.Row="3" Grid.ColumnSpan="2">
                        </ItemsControl>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
    </Style>

    <Style TargetType="primitives:UtilityAssociationGroupResultPopupView">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <!-- x:DataType=UtilityAssociationResult-->
                    <primitives:UtilityAssociationResultPopupView AssociationResult="{Binding}" x:Name="ResultView" MinHeight="40"  AutomationProperties.Name="{Binding Title}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:UtilityAssociationGroupResultPopupView">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Button x:Name="ExpandIcon" Background="{TemplateBinding Background}" BorderThickness="0"
                                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}">
                            <FontIcon Glyph="&#xE79B;" VerticalAlignment="Center" Margin="5" FontFamily="{StaticResource toolkit-icons}"/>
                        </Button>
                        <TextBlock x:Name="Title" Text="{Binding Name}" Style="{StaticResource PopupViewerTitleStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Grid.Column="1"
                                   Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <Border Width="30" Height="20" CornerRadius="10" Background="LightGray" Grid.Column="2" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}">
                            <TextBlock Text="{Binding AssociationResults.Count}" HorizontalAlignment="Center" />
                        </Border>
                        <Border BorderThickness="0" Height="1" Margin="2" Grid.Row="1" Grid.ColumnSpan="3" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"/>
                        <Grid x:Name="Search" Grid.ColumnSpan="3" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}}">
                            <Border BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <FontIcon Glyph="&#xE71E;" Margin="2" VerticalAlignment="Center"/>
                                    <TextBox x:Name="SearchText" PlaceholderText="{internal:LocalizedString Key=PopupViewerUtilityAssociationsFilterByTitle}" Margin="2" BorderThickness="0" Grid.Column="1"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ListView VerticalAlignment="Top" ItemTemplate="{TemplateBinding ItemTemplate}" ItemsSource="{Binding GroupResult.AssociationResults, RelativeSource={RelativeSource Mode=TemplatedParent}, Mode=OneWay}" x:Name="ResultsList" SelectionMode="None" IsItemClickEnabled="True" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="3">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                        <Button x:Name="ShowAll" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="4" Margin="10,0,0,0" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSearchable, Converter={StaticResource PopupViewerVisibilityConverter}, ConverterParameter=Reverse}"
                                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch"
                                Background="{TemplateBinding Background}" BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height=".25*" />
                                    <RowDefinition Height=".25*" />
                                    <RowDefinition Height=".25*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{internal:LocalizedString Key=PopupViewerUtilityAssociationsShowAll}" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" FontSize="12"/>
                                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{internal:LocalizedString Key=PopupViewerUtilityAssociationsTotal}" Style="{StaticResource PopupViewerCaptionStyle}" VerticalAlignment="Top" Margin="0,0,2,0" />
                                    <TextBlock Text="{Binding AssociationResults.Count}" VerticalAlignment="Top" Style="{StaticResource PopupViewerCaptionStyle}" />
                                </StackPanel>
                                <FontIcon Glyph="&#xE8FD;" VerticalAlignment="Stretch" FontSize="18" Grid.Column="1" Grid.RowSpan="2" Margin="2"/>
                                <Border BorderThickness="0" Height="1" Background="LightGray" Margin="2" Grid.ColumnSpan="3" Grid.Row="2"/>
                            </Grid>
                        </Button>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
    </Style>

    <Style TargetType="primitives:UtilityAssociationResultPopupView">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:UtilityAssociationResultPopupView">
                    <Grid Margin="10,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <FontIcon x:Name="Icon" Width="20" VerticalAlignment="Center" Margin="5" Grid.RowSpan="2" FontFamily="{StaticResource toolkit-icons}"/>
                        <TextBlock x:Name="Title" TextWrapping="NoWrap" Text="{Binding AssociationResult.Title, RelativeSource={RelativeSource Mode=TemplatedParent}}" Style="{StaticResource PopupViewerTitleStyle}"
                                   VerticalAlignment="Center" Grid.Column="1" TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="FractionAlong" Visibility="Collapsed" Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding ElementName=FractionAlong, Path=Text}" />
                        <TextBlock x:Name="ConnectionInfo" Visibility="Collapsed" Grid.Column="1" Grid.Row="1" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding ElementName=ConnectionInfo, Path=Text}" Style="{StaticResource PopupViewerCaptionStyle}"/>
                        <FontIcon Glyph="&#xE76C;" Grid.Column="3" VerticalAlignment="Center" Margin="4,0" Grid.RowSpan="2" />
                        <Border BorderThickness="0" Height="1" Background="LightGray" Margin="2" Grid.Row="2" Grid.ColumnSpan="4"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="primitives:NavigationSubView">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="primitives:NavigationSubView">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="NavigateBack" Background="Transparent" BorderThickness="0,0,2,0" VerticalAlignment="Stretch" CornerRadius="0" MinHeight="50">
                                <FontIcon Glyph="&#xE76B;" />
                            </Button>
                            <Button x:Name="NavigateUp" Background="Transparent" BorderThickness="0" VerticalAlignment="Stretch" MinHeight="50">
                                <FontIcon Glyph="&#xE74A;" />
                            </Button>
                            <ContentControl ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}" Padding="{TemplateBinding Padding}" Margin="12,0,0,0"
                                            VerticalAlignment="Stretch" VerticalContentAlignment="Center" x:Name="Header" />
                        </StackPanel>
                        <ScrollViewer x:Name="ScrollViewer" VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" Grid.Row="1" IsVerticalRailEnabled="True" Padding="{TemplateBinding Padding}" >
                            <ContentControl x:Name="Content"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="controls:PopupViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:PopupViewer">
                    <Border Background="{TemplateBinding Background}"
                   BorderBrush="{TemplateBinding BorderBrush}"
                   BorderThickness="{TemplateBinding BorderThickness}"
                   Margin="{TemplateBinding Margin}"
                   MinWidth="{TemplateBinding MinWidth}"
                   MaxWidth="{TemplateBinding MaxWidth}">
                        <primitives:NavigationSubView x:Name="SubFrameView" VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" Padding="{TemplateBinding Padding}">
                            <primitives:NavigationSubView.HeaderTemplateSelector>
                                <primitives:PopupContentTemplateSelector>
                                    <primitives:PopupContentTemplateSelector.PopupTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Title, Converter={StaticResource PopupViewerHtmlToPlainTextConverter}, Mode=OneWay}" Style="{StaticResource PopupViewerHeaderStyle}"
                                                Visibility="{Binding Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.PopupTemplate>
                                    <primitives:PopupContentTemplateSelector.UtilityAssociationsFilterResultTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Filter.Title, Converter={StaticResource PopupViewerHtmlToPlainTextConverter}, Mode=OneWay}" Style="{StaticResource PopupViewerHeaderStyle}"
                                                Visibility="{Binding Filter.Title, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                                                <TextBlock Text="{Binding Filter.Description, Converter={StaticResource PopupViewerHtmlToPlainTextConverter}, Mode=OneWay}" Style="{StaticResource PopupViewerTitleStyle}"
                                                Visibility="{Binding Filter.Description, Converter={StaticResource PopupViewerVisibilityConverter}}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.UtilityAssociationsFilterResultTemplate>
                                    <primitives:PopupContentTemplateSelector.UtilityAssociationGroupResultTemplate>
                                        <DataTemplate>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name, Mode=OneWay}" Style="{StaticResource PopupViewerHeaderStyle}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.UtilityAssociationGroupResultTemplate>
                                </primitives:PopupContentTemplateSelector>
                            </primitives:NavigationSubView.HeaderTemplateSelector>
                            <primitives:NavigationSubView.ContentTemplateSelector>
                                <primitives:PopupContentTemplateSelector>
                                    <primitives:PopupContentTemplateSelector.PopupTemplate>
                                        <DataTemplate>
                                            <Grid DataContext="{Binding}">
                                                <primitives:PopupElementItemsControl ItemsSource="{Binding EvaluatedElements, Mode=OneWay}" Margin="0,10" x:Name="ItemsView">
                                                    <primitives:PopupElementItemsControl.TextPopupElementTemplate>
                                                        <DataTemplate>
                                                            <primitives:TextPopupElementView Element="{Binding}" Margin="0,10" />
                                                        </DataTemplate>
                                                    </primitives:PopupElementItemsControl.TextPopupElementTemplate>
                                                    <primitives:PopupElementItemsControl.MediaPopupElementTemplate>
                                                        <DataTemplate>
                                                            <primitives:MediaPopupElementView Element="{Binding}" Margin="0,10" />
                                                        </DataTemplate>
                                                    </primitives:PopupElementItemsControl.MediaPopupElementTemplate>
                                                    <primitives:PopupElementItemsControl.FieldsPopupElementTemplate>
                                                        <DataTemplate>
                                                            <primitives:FieldsPopupElementView Element="{Binding}" Margin="0,10" />
                                                        </DataTemplate>
                                                    </primitives:PopupElementItemsControl.FieldsPopupElementTemplate>
                                                    <primitives:PopupElementItemsControl.AttachmentsPopupElementTemplate>
                                                        <DataTemplate>
                                                            <primitives:AttachmentsPopupElementView Element="{Binding}" Margin="0,10" />
                                                        </DataTemplate>
                                                    </primitives:PopupElementItemsControl.AttachmentsPopupElementTemplate>
                                                    <primitives:PopupElementItemsControl.UtilityAssociationsPopupElementTemplate>
                                                        <DataTemplate>
                                                            <!--x:DataType="popup:UtilityAssociationsPopupElement"-->
                                                            <primitives:UtilityAssociationsPopupElementView Element="{Binding}" Margin="0,10" />
                                                        </DataTemplate>
                                                    </primitives:PopupElementItemsControl.UtilityAssociationsPopupElementTemplate>
                                                </primitives:PopupElementItemsControl>
                                            </Grid>
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.PopupTemplate>
                                    <primitives:PopupContentTemplateSelector.UtilityAssociationsFilterResultTemplate>
                                        <DataTemplate>
                                            <primitives:UtilityAssociationsFilterResultsPopupView AssociationsFilterResult="{Binding}" IsExpanded="True"/>
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.UtilityAssociationsFilterResultTemplate>
                                    <primitives:PopupContentTemplateSelector.UtilityAssociationGroupResultTemplate>
                                        <DataTemplate>
                                            <primitives:UtilityAssociationGroupResultPopupView GroupResult="{Binding}" IsSearchable="True"/>
                                        </DataTemplate>
                                    </primitives:PopupContentTemplateSelector.UtilityAssociationGroupResultTemplate>
                                </primitives:PopupContentTemplateSelector>
                            </primitives:NavigationSubView.ContentTemplateSelector>
                        </primitives:NavigationSubView>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
