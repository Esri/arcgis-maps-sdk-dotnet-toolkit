<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="Esri.ArcGISRuntime.Toolkit.OfflineMapAreasViewResources"
    xmlns:esri="using:Esri.ArcGISRuntime.UI.Controls"
    xmlns:controls="using:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:local="using:Esri.ArcGISRuntime.Toolkit">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../../Themes/SharedResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="OfflineMapAreasViewButtonStyle" TargetType="Button">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>
    <Style TargetType="controls:OfflineMapAreasView">
        <!--  Default item template can be overriden by the user.  -->
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate x:DataType="controls:IOfflineMapAreaItem">
                    <Grid Height="64" Margin="0,4" AutomationProperties.HelpText="{x:Bind Title, Mode=OneWay}" AutomationProperties.FullDescription="{x:Bind Description, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="68" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border CornerRadius="8" Grid.RowSpan="3" BorderThickness="1" BorderBrush="LightGray" Margin="0,0,4,0">
                            <Image Source="{x:Bind local:OfflineMapAreasViewResources.BytesToImage(ThumbnailData), Mode=OneWay}" Width="64" Height="64" Stretch="UniformToFill"/>
                        </Border>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind Title, Mode=OneWay}" TextTrimming="CharacterEllipsis" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind Description, Mode=OneWay}" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" MaxLines="3" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Text="Downloaded" Grid.Row="2" Grid.Column="1" Visibility="{x:Bind IsDownloaded, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" />
                        <Grid Grid.Column="2" Grid.RowSpan="3">
                            <Button Style="{StaticResource OfflineMapAreasViewButtonStyle}" AutomationProperties.HelpText="Stop" ToolTipService.ToolTip="Stop" Command="{x:Bind StopDownloadCommand, Mode=OneWay}" Visibility="{x:Bind IsDownloading, Mode=OneWay}">
                                <SymbolIcon Symbol="Stop" />
                            </Button>
                            <Button Style="{StaticResource OfflineMapAreasViewButtonStyle}" AutomationProperties.HelpText="Download" ToolTipService.ToolTip="Download" Command="{x:Bind DownloadCommand, Mode=OneWay}" Visibility="{x:Bind AllowsDownload, Mode=OneWay}">
                                <SymbolIcon Symbol="Download" />
                            </Button>
                            <ProgressRing Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False" Minimum="0" Maximum="1" IsIndeterminate="False" Value="{x:Bind DownloadProgress, Mode=OneWay}" Visibility="{x:Bind IsDownloading, Mode=OneWay}" />
                            <StackPanel Orientation="Horizontal" Visibility="{x:Bind IsDownloaded, Mode=OneWay}" >
                                <Button Style="{StaticResource OfflineMapAreasViewButtonStyle}" ToolTipService.ToolTip="Remove" AutomationProperties.HelpText="Remove" Command="{x:Bind RemoveDownloadCommand, Mode=OneWay}">
                                    <SymbolIcon Symbol="Delete" />
                                </Button>
                                <Button Content="Open" AutomationProperties.HelpText="Open" Margin="8,0,0,0" Command="{x:Bind OpenCommand, Mode=OneWay}" CommandParameter="{x:Bind Map, Mode=OneWay}" />
                            </StackPanel>
                            <Button Style="{StaticResource OfflineMapAreasViewButtonStyle}" AutomationProperties.HelpText="{x:Bind Error.Message, Mode=OneWay}" ToolTipService.ToolTip="{x:Bind Error.Message, Mode=OneWay}"
                                    Command="{x:Bind RemoveDownloadCommand, Mode=OneWay}" Visibility="{x:Bind local:OfflineMapAreasViewResources.VisibleIfNotNull(Error), Mode=OneWay}"
                                    >
                                <FontIcon Glyph="&#xE783;" />
                            </Button>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:OfflineMapAreasView">
                    <Border
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                        <Border.Resources>
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource ControlFillColorDefault}" />
                        </Border.Resources>
                        <Grid>
                            <ScrollViewer VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" Visibility="{x:Bind local:OfflineMapAreasViewResources.FalseToVisible(TemplateSettings.IsAddOnDemandMode), Mode=OneWay}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ItemsControl x:Name="MapAreasView" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                                        ItemsSource="{x:Bind TemplateSettings.MapAreas, Mode=OneWay}" ItemTemplate="{TemplateBinding ItemTemplate}"/>

                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{x:Bind TemplateSettings.MapIsOfflineDisabled, Mode=OneWay}">
                                        <TextBlock
                                            Grid.Column="0" Foreground="{StaticResource TextFillColorTertiaryBrush}"
                                            HorizontalAlignment="Center"
                                            FontFamily="{StaticResource toolkit-icons}"
                                           Text="&#xE846;" FontSize="48" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="Offline Disabled" TextTrimming="CharacterEllipsis" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="The map is not enabled for offline use" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Foreground="{StaticResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>

                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{x:Bind TemplateSettings.IsInternetNotAvailable, Mode=OneWay}">
                                        <TextBlock
                                            Grid.Column="0" Foreground="{StaticResource TextFillColorTertiaryBrush}"
                                            HorizontalAlignment="Center"
                                            FontFamily="{StaticResource toolkit-icons}"
                                           Text="&#xE846;" FontSize="48" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="No Internet Connection" TextTrimming="CharacterEllipsis" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="Could not retrieve map areas for this map" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Foreground="{StaticResource TextFillColorSecondaryBrush}" />
                                        <Button HorizontalAlignment="Center" AutomationProperties.HelpText="Refresh" x:Name="NoInternetRefreshButton">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xEB66;" FontFamily="{StaticResource toolkit-icons}" VerticalAlignment="Center" FontSize="14" Margin="0,0,4,0" />
                                                <TextBlock Text="Refresh"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>

                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{x:Bind TemplateSettings.HasNoAreas, Mode=OneWay}">
                                        <TextBlock
                                        Grid.Column="0" Foreground="{StaticResource TextFillColorTertiaryBrush}"
                                        HorizontalAlignment="Center"
                                        FontFamily="{StaticResource toolkit-icons}"
                                       Text="&#xE81C;" FontSize="48" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="No Map Areas" TextTrimming="CharacterEllipsis" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="There are no map areas for this map." Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                               Visibility="{x:Bind TemplateSettings.IsPreplannedMode, Mode=OneWay}" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="There are no map areas for this map. Tap the button below to get started." Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                               Visibility="{x:Bind TemplateSettings.IsOnDemandMode, Mode=OneWay}" />
                                        <Button HorizontalAlignment="Center" Visibility="{x:Bind TemplateSettings.IsPreplannedMode, Mode=OneWay}" AutomationProperties.HelpText="Refresh" x:Name="RefreshMapAreasButton">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xEB66;" FontFamily="{StaticResource toolkit-icons}" VerticalAlignment="Center" FontSize="14" Margin="0,0,4,0" />
                                                <TextBlock Text="Refresh"/>
                                            </StackPanel>
                                        </Button>

                                    </StackPanel>

                                    <ProgressRing Visibility="{x:Bind TemplateSettings.IsLoadingModels, Mode=OneWay}" IsHitTestVisible="False" />

                                    <Grid Grid.Row="1" Visibility="{x:Bind local:OfflineMapAreasViewResources.FalseToVisible(TemplateSettings.IsInternetNotAvailable), Mode=OneWay}" >
                                        <Button HorizontalAlignment="Center" Visibility="{x:Bind TemplateSettings.IsOnDemandMode, Mode=OneWay}" AutomationProperties.HelpText="Add Map Area" x:Name="AddMapAreaButton" Margin="4" Padding="24,4">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE9F6;" FontFamily="{StaticResource toolkit-icons}" FontSize="20" VerticalAlignment="Center" Margin="0,2,0,0" />
                                                <TextBlock Text="Add Map Area" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </ScrollViewer>
                            <!-- Add Map Area View -->
                            <Grid Visibility="{x:Bind TemplateSettings.IsAddOnDemandMode, Mode=OneWay}" RowSpacing="8" Margin="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition MaxHeight="400" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Select Area" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <Grid Grid.Row="1" >
                                    <esri:MapView x:Name="AddAreaMapView" Grid.Row="1" IsAttributionTextVisible="False" BorderBrush="Black" BorderThickness="1" x:Load="False" />
                                    <Border BorderBrush="Black" BorderThickness="40" IsHitTestVisible="False" Opacity=".5" />
                                </Grid>
                                <TextBox PlaceholderText="Name" Grid.Row="2" x:Name="AddOnDemandAreaNameTextBox" />
                                <Button Content="Add" Grid.Row="3" HorizontalAlignment="Stretch" x:Name="AcceptAddOnDemandAreaButton" />
                                <Button Content="Cancel" HorizontalAlignment="Right" x:Name="CancelAddOnDemandAreaButton" />

                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
    </Style>
</ResourceDictionary>
