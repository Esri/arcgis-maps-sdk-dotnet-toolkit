<Page
    x:Class="Esri.ArcGISRuntime.Toolkit.SampleApp.Samples.Jobs.JobManagerSample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Esri.ArcGISRuntime.Toolkit.SampleApp.Samples.Jobs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:esri="using:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:tasks="using:Esri.ArcGISRuntime.Tasks"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="8,24">
         Jobs are automatically persisted across application sessions. You can start a job below and shut down the app. When the app resumes, the jobs will also auto-resume.
        </TextBlock>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Generate a geodatabase" Click="GenerateGdb_Click" Margin="8" Padding="4" Style="{StaticResource CalcitePrimaryButtonStyle}" />
            <Button Content="Generate an offline map" Click="GenerateMap_Click" Margin="8" Padding="4" Style="{StaticResource CalcitePrimaryButtonStyle}" />
            <Button Content="Clear completed jobs" Click="Clear_Click" Margin="8" Padding="4" Style="{StaticResource CalciteDangerSecondaryButtonStyle}" />
        </StackPanel>

        <ListView x:Name="JobCollectionView" Grid.Row="2" ItemsSource="{x:Bind JobManager.Jobs, Mode=OneWay}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  BorderBrush="{ThemeResource CalciteBrandBrush}" BorderThickness="1" Margin="8" Padding="8,16"
                  >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="tasks:IJob" >
                    <Border BorderBrush="Gray" >
                        <local:JobViewControl Job="{x:Bind}" HorizontalAlignment="Stretch" />
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

